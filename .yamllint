extends: default

rules:
  line-length:
    max: 120
    level: warning

  # Allow longer lines in GitHub workflows as they often contain long URLs and commands
  line-length:
    max: 200
    level: warning
    ignore: |
      .github/workflows/*.yml
